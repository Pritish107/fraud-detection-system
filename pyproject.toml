[tool.ruff]
line-length = 110
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = [
    "E402",  # sys.path.insert(...) before local imports is intentional throughout src/
]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["E402"]
